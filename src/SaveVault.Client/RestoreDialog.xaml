<Window x:Class="SaveVault.Client.RestoreDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Wiederherstellen" Height="240" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource WindowBgBrush}"
        TextOptions.TextFormattingMode="Ideal"
        UseLayoutRounding="True">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Version wiederherstellen" FontSize="17" FontWeight="Bold" />

        <TextBlock Grid.Row="1" x:Name="MessageText" Text=""
                   Foreground="{StaticResource TextBrush}" FontSize="13"
                   TextWrapping="Wrap" Margin="0,14,0,0" VerticalAlignment="Top" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Abbrechen" Style="{StaticResource SecondaryButton}" Padding="18,8"
                    Click="OnCancelClick" Margin="0,0,10,0" />
            <Button Content="Wiederherstellen" Style="{StaticResource PrimaryButton}" Padding="18,8"
                    Click="OnConfirmClick" />
        </StackPanel>
    </Grid>
</Window>
